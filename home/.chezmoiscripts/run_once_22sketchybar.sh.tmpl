{{- $versions := joinPath .chezmoi.sourceDir "versions.yaml" | include | fromYaml -}}
#!/bin/bash
{{- if eq .chezmoi.os "darwin" }}
# Hash version {{ $versions.versions.sketchybar | sha256sum }}
# Hash directory {{ $versions.checksums.sketchybar }}
brew services restart sketchybar
{{- end }}
